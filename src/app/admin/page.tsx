import { Metadata } from 'next';

const AdminHomePage = () => {
  return <section className="mx-auto w-11/12">Admin</section>;
};

export default AdminHomePage;

// >> SEO Start
export const metadata: Metadata = {
  title: 'Dashboard | Aminul Islam',
};
// >> SEO End
